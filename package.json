{
  "name": "penecho",
  "version": "0.1.0",
  "private": true,
  "description": "Sparse 20k handwritten AI canvas with editable visual responses",
  "license": "AGPL-3.0-only",
  "keywords": ["canvas", "handwriting", "ai", "whiteboard", "math"],
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check codex-cli.js && node --check public/draw.js && node --check public/app.js && node --test",
    "test": "node --test"
  },
  "engines": { "node": ">=18" }
}
